(jbuild_version 1)

(executables
 ((names (scilla_runner eval_runner))
  (public_names (scilla-runner eval-runner))
  (modules (scilla_runner eval_runner))
  (libraries (core
              angstrom
              ppx_sexp_conv ppx_deriving
              ppx_let yojson cryptokit
              scilla-syntax scilla-eval))
  (preprocess (pps (ppx_sexp_conv ppx_let ppx_deriving.show)))
  (package scilla-runner)))
