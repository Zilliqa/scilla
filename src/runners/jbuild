(jbuild_version 1)

(executables
 ((names (scilla_runner scilla_exp_eval))
  (public_names (scilla-runner scilla-exp-eval))
  (modules (scilla_runner scilla_exp_eval))
  (libraries (core scilla-lang ppx_sexp_conv))
  (preprocess (pps (ppx_sexp_conv)))
  (package scilla-runner)))
