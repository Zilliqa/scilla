(jbuild_version 1)

(executables
 ((names (scilla_runner))
  (public_names (scilla-runner))
  (modules (scilla_runner))
  (libraries (core scilla-lang ppx_sexp_conv))
  (preprocess (pps (ppx_sexp_conv)))
  (package scilla-runner)))
