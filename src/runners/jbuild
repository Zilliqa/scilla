(jbuild_version 1)

(executables
 ((names (scilla_runner eval_runner scilla_checker))
  (public_names (scilla-runner eval-runner scilla-checker))
  (modules (scilla_runner
            eval_runner
            scilla_checker
            cli))
  (libraries (core
              angstrom
              ppx_sexp_conv ppx_deriving
              ppx_let yojson cryptokit
              scilla-base scilla-eval))
  (preprocess (pps (ppx_sexp_conv ppx_let ppx_deriving.show)))
  (package scilla-runner)))
