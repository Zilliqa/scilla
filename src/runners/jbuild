(jbuild_version 1)

(executables
 ((names (scilla_runner scilla_stmt_parse scilla_module_parse))
  (public_names (scilla-runner scilla-stmt-parse scilla-module-parse))
  (modules (scilla_runner scilla_stmt_parse scilla_module_parse))
  (libraries (core scilla-lang ppx_sexp_conv))
  (preprocess (pps (ppx_sexp_conv)))
  (package scilla-runner)))
