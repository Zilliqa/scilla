(jbuild_version 1)

(ocamllex (ScillaLexer))

(menhir
 ((modules (ScillaParser))))

(library
 ((name scilla_lang) ; doesn't support '-' in the name
  (public_name scilla-lang)
  (wrapped false)
  (libraries (core ppx_sexp_conv yojson cryptokit))
  (preprocess (pps (ppx_sexp_conv)))
  (synopsis "Scilla workbench implementation.")))
