(jbuild_version 1)

(library
 ((name scilla_eval)
  (public_name scilla-eval)
  (wrapped false)
  (libraries (core ppx_sexp_conv ppx_deriving
              ppx_let yojson cryptokit scilla-syntax))
  (preprocess (pps (ppx_sexp_conv ppx_let ppx_deriving.show)))
  (synopsis "Scilla workbench implementation.")))
