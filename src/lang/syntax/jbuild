(jbuild_version 1)

(ocamllex (ScillaLexer))

(menhir
 ((modules (ScillaParser))))

(library
 ((name scilla_syntax) ; doesn't support '-' in the name
  (public_name scilla-syntax)
  (wrapped false)
  (libraries (core ppx_sexp_conv yojson))
  (preprocess (pps (ppx_sexp_conv)))
  (synopsis "Scilla workbench implementation.")))
