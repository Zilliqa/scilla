(* Expected result: 42 *)
let v = 42 in
let y = Some {Int} v in
let z = 3 in 
let b = True in
let c = Some {Option Int} y in
match b with 
    | False => 1
    | True  => match c with 
               | Some (Some x) => x
               | None  => z
               end
    end
